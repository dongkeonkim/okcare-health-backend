# 오케어 백엔드 과제 작업 원칙

## 항상 적용하는 원칙

- 항상 한국어로 답변한다.
- 작업 전에 현재 디렉터리, 파일 구조, 빌드 설정과 기존 변경 상태를 확인한다.
- 파괴적 명령과 데이터 삭제는 사용자가 명시적으로 요청한 경우에만 수행한다.
- 비밀번호, 토큰, 자격 증명, 개인정보와 실사용 건강 데이터 payload를 로그·오류·공개 문서에 남기지 않는다.

## 구현 원칙

- **코딩 전에 생각하기** — 애매한 내용을 마음대로 가정하지 않고 구현이나 리뷰 결론 전에 사용자에게 확인한다.
- **단순하게 구현하기** — 목적 달성에 필요하지 않은 기능, 추상화, 확장 구조와 과잉 방어를 추가하지 않는다.
- **필요한 부분만 수정하기** — 요청과 무관한 코드와 문서를 건드리지 않고 기존 사용자 변경을 보존한다.
- **검증 가능한 목표로 작업하기** — 테스트 또는 성공 조건을 먼저 정하고 완료 여부를 확인한다.

## 주석 원칙

코드, 설정, migration과 빌드 스크립트 전부에 적용한다.

주석은 코드가 스스로 말할 수 없는 것만 적는다. 코드는 무엇을 어떻게 하는지 이미 정확하게, 그리고 항상 최신 상태로 표현한다. 주석이 담당할 것은 셋뿐이다.

- **버린 대안** — 더 단순해 보이는 방법을 왜 쓰지 않았는가
- **외부 제약** — 라이브러리 동작, 기능 계약과 스키마가 왜 이 모양을 강요했는가
- **함정** — 다음 사람이 무심코 고쳤다가 무엇을 깨뜨리는가

판별 기준은 하나다. **이 주석을 지웠을 때 코드만 보고 복원할 수 없는 정보가 사라지는가.** 아니면 지운다.

- 모든 주석은 한국어로 쓰고 `~한다`가 아니라 명사형으로 끊는다. `빈으로 노출한다.`가 아니라 `빈으로 노출.`
- 코드를 다시 설명하는 주석을 쓰지 않는다. 유지 비용만 늘고 코드가 바뀌면 조용히 거짓말이 된다.
- 클래스와 메서드마다 Javadoc을 붙이지 않는다. 위 세 가지에 해당할 때만 붙인다.
- 근거 문서의 파일명과 절 번호를 적지 않고 판단의 이유 자체를 적는다. 절 번호는 문서를 개편하면 조용히 어긋나고, 코드만 읽는 사람은 확인할 수 없다.
- 주석이 없는 코드가 정상이다. 주석이 몰린 자리가 까다로운 지점이라는 신호로 읽히도록 밀도를 유지한다.
- 테스트에는 무엇을 검증하는지가 아니라 왜 그것을 검증해야 하는지를 적는다.

## 코드 서식

포매터 플러그인을 두지 않으므로 아래를 손으로 지킨다. 서식은 취향이 아니라 규칙이고, 같은 상황은 같은 모양으로 쓴다.

- 한 줄은 화면 기준 100칸을 넘지 않는다. 한국어는 두 칸을 차지하므로 글자 수가 아니라 표시 폭으로 센다.
- SQL DDL의 컬럼 정의는 예외다. 인라인 `COMMENT` 때문에 길어져도 줄을 바꾸지 않는다. 정렬된 표 형태가 끊어진 줄보다 읽기 쉽다.
- 들여쓰기는 스페이스 4칸, 줄이 이어지면 8칸을 쓴다. 탭을 쓰지 않는다.
- 한 요소에 애노테이션이 둘 이상이면 각각을 별도 줄에 둔다. 하나뿐이고 100자 안에 들어가면 같은 줄에 둔다.
- 애노테이션이 붙은 `record` 컴포넌트와 메서드 파라미터는 애노테이션과 타입·이름을 같은 열에 맞추고, 요소 사이를 빈 줄로 끊는다. 닫는 괄호는 별도 줄에 둔다.
- 메서드 체인이 100자를 넘으면 `.` 앞에서 끊고 8칸 들여쓴다.

## 요구사항과 비즈니스 범위 통제

- [`과제_원문.md`](./docs/요구사항/과제_원문.md)를 반드시 충족해야 하는 기능, 기술과 제출물의 최상위 요구사항으로 삼는다.
- [`비즈니스_기획.md`](./docs/요구사항/비즈니스_기획.md)는 과제 원문과 사용자가 승인한 변경 안에서 구현할 제품 기능의 범위를 정의한다.
- 기능 명세와 개발 계획은 승인된 범위를 구체화할 뿐 새로운 기능, 정책, 상태 전이와 대체 흐름으로 확장하지 않는다.
- 과제 원문의 요구사항이 하위 문서에서 누락되면 범위 밖으로 판단하지 않고 알맞은 문서를 먼저 보완한다.
- 과제 원문과 비즈니스 기획에 없고 사용자가 승인하지 않은 기능이나 개선은 구현하지 않고 리뷰 요구사항으로 제안하지 않는다.
- 인증, 소유권, 데이터 정합성과 비밀정보 보호처럼 비즈니스 목적을 달성하는 데 필요한 방어만 위험에 비례해 적용한다.

## 프로젝트 스킬 라우팅

- Java/Spring/API/DB/Redis/Docker 구현·수정·디버깅에는 [`develop-backend`](./.agents/skills/develop-backend/SKILL.md)를 사용한다.
- 코드·설정·migration·Docker 변경, diff, PR과 구현 경계 리뷰에는 [`review-backend`](./.agents/skills/review-backend/SKILL.md)를 사용한다.
- 테스트 설계·작성·수정·리뷰에는 [`write-backend-tests`](./.agents/skills/write-backend-tests/SKILL.md)를 사용한다.
- 테스트 실행, 회귀·E2E와 Docker 검증에는 [`verify-backend`](./.agents/skills/verify-backend/SKILL.md)를 사용한다.
- 기획·기능·개발·API·ERD·검증 문서 작업에는 [`maintain-project-docs`](./.agents/skills/maintain-project-docs/SKILL.md)를 사용한다.
- Git 변경 검토, 스테이징과 커밋에는 [`manage-git-commits`](./.agents/skills/manage-git-commits/SKILL.md)를 사용한다.
- Orca 작업 분해와 병렬 탭 운영에는 [`orchestrate-work`](./.agents/skills/orchestrate-work/SKILL.md)를 사용한다.
- 한 요청이 여러 유형에 걸치면 관련 스킬을 모두 적용한다.
- 스킬이 선택한 문서는 일부만 추측해 적용하지 않고 전체를 읽는다.
- 문서에 없는 정책이나 문서 간 충돌은 사용자에게 확인한다.

## 변경과 커밋 통제

- [`docs/설계/개발_계획.md`](./docs/설계/개발_계획.md)의 리뷰 경계에 도달하면 결과를 보고하고 멈춘다.
- 사용자가 `커밋해`처럼 명시적으로 요청하기 전에는 커밋하지 않는다.
- 파괴적이지 않은 범위에서도 사용자 요청과 다른 외부 상태 변경은 임의로 수행하지 않는다.
